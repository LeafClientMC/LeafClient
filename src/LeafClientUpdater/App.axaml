<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:LeafClientUpdater.Views"
             x:Class="LeafClientUpdater.App">
	<Application.Styles>
		<!-- AOT-FIX: Removed the "Mode" property. Theme is now controlled by RequestedThemeVariant -->
		<FluentTheme />
	</Application.Styles>

	<Application.RequestedThemeVariant>
		Dark
	</Application.RequestedThemeVariant>

	<!-- AOT-FIX: Removed the Application.MainWindow block. 
         The main window is set in App.axaml.cs, which is the correct practice. -->
</Application>
